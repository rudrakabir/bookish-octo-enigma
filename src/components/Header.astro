---
const pages = ['about', '1', 'blog', 'blogindex', 'index', 'tester'];
---

<nav class="bg-blue-600 text-white p-4">
    {pages.map(page => (
        <a href={`/${page}`} class="mr-4">{page.charAt(0).toUpperCase() + page.slice(1)}</a>
    ))}
</nav>
